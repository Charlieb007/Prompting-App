// The single source of truth for where the extension sends its API calls.
// Production backend on Render — change this if you move to a different host.
window.PROMPT_REFINERY_CONFIG = {
  apiUrl: 'https://prompt-refina-api.onrender.com',
};
